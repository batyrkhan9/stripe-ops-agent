import { Placeholder } from "@/components/placeholder";

export default function ActionsPage() {
  return (
    <Placeholder
      title="Actions"
      description="Proposed actions waiting for confirmation, and the audit log of every tool call."
    />
  );
}
