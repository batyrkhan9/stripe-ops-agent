import { Placeholder } from "@/components/placeholder";

export default function SettingsPage() {
  return (
    <Placeholder
      title="Settings"
      description="Connect a Stripe account with read-only access, and manage API tokens."
    />
  );
}
