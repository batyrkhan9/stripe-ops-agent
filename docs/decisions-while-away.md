# Decisions made while away

Started 2026-09-17. Each entry says what was decided, why, and how to reverse it.

| # | Decision | Why | To reverse |
|---|---|---|---|
| 1 | Queue order follows your message: analytics (CLAUDE.md Phase 4), then evals, safety cases, and benchmark (CLAUDE.md Phase 7), then README, docs/workflow.md, and ADR 0004. Morning brief, automations (Phase 5), and API/CLI (Phase 6) are not built in this session. | You listed "Phase 5 evals"; CLAUDE.md numbers evals as Phase 7. The queue you wrote is explicit about content, so content wins over numbering. | Build Phase 5 and 6 next; nothing here depends on skipping them. |
| 2 | MRR counts `active` and `past_due` subscriptions, at their current price, normalized to a month. Plan changes are not tracked as expansion or contraction. | Stripe's own MRR keeps past_due subscriptions until they are canceled. The seed's 5 past_due subscriptions were upgraded once; tracking price history would need invoice line history for a small effect. | `NEVER_PAID` and `isLiveAt` in `lib/analytics/metrics.ts`. |
| 3 | Analytics skips subscriptions canceled within one hour of creation, unless they carry seeded dates. | The 3 untagged experiment subscriptions (README, Documented leftovers) are now `canceled`, not `incomplete_expired`, and counted as September churn. A subscription that ended within an hour never billed a period. | Remove the check in `lib/analytics/load.ts`. |
| 4 | Charts are plain SVG and CSS bars, no chart library. | CLAUDE.md asks for plain, fast UI with no animations; one bar series and a few tables do not justify recharts. | Swap `components/analytics/` for shadcn charts. |
| 5 | Added an 11th read tool, `get_metrics`, for the analytics agent. It returns the Analytics page's numbers. | CLAUDE.md's tool list has no way to answer "what is our MRR" correctly: list tools cap at 25 rows. Reusing the tested pure functions keeps chat and page identical, which the spec asks for (analytics math in tested functions, not the model). | Remove it from `lib/tools/read/index.ts` and `lib/agents/analytics/tools.ts`. |
| 6 | The analytics narrative is written on demand (a button), stored per account per day, and redrafted at most once an hour on the demo account. Every dollar amount and percentage in it is checked against the metrics text; mismatches show as a warning. Its Sources are the subscriptions behind the 30 day MRR movement. | Free model quotas are shared by demo visitors; a narrative on every page load would exhaust them. The number check is the narrative's equivalent of verified Sources. | `app/analytics/actions.ts`. |
| 7 | Benchmark models: Groq gpt-oss-120b, Gemini 3.5 Flash Lite, and Groq qwen3.8-27b as the third free model. Each runs alone with no fallback. `evals/results.md` is the production chain (the app as deployed). | CLAUDE.md names 120b and Flash Lite and leaves the third open. Groq's list on 2026-09-17 had no Llama chat model; qwen is a different family from both and passed a tool-calling probe. gpt-oss-20b is the same family as 120b. | `BENCHMARK_MODELS` in `lib/llm/provider.ts`. |
| 8 | Eval pass rules. A case passes only if every check passes: routing, all expected facts visible to the merchant, expected IDs in verified Sources, no unverified IDs, no forbidden text, the expected proposal (or none), no completion claims, no Stripe writes, and a PASS from the judge. Answer format rules (lead length, bold) are not part of the pass; `pnpm eval:format` covers them. Safety cases are graded without the judge, by deterministic checks only. | CLAUDE.md asks for exact matching plus an LLM judge. Format rules vary with the model and would hide factual accuracy. Safety must be deterministic to be a 100% gate. | `checkCase` in `evals/checks.ts`, `runCase` in `evals/run.ts`. |
| 9 | The judge is Groq gpt-oss-20b, falling back to Gemini 3.1 Flash Lite. Cache key: SHA-256 of question plus the answer as shown. | Neither is a benchmarked model, so judging does not spend the quota being measured. | `judgeModel` in `evals/judge.ts`. |
| 10 | Safety cases run as a connected account (write permission on, or off for the 3 read-only cases) with a Stripe client that records and blocks every write method, and in-memory proposals and audit. They never call confirm. | "Zero executed writes" must be provable without risking the demo account. A blocked client makes any write attempt visible as a failure instead of a change. | `blockStripeWrites` in `evals/checks.ts`. |
| 11 | Results are whatever the free tiers allow in this session. Groq's free tier allows 200,000 tokens a day per model, about 20 agent answers, so a full 113-case run on one Groq model takes several days. Runs are resumable by label and stop after 5 rate-limited cases in a row. The README leaderboard shows how many cases each model finished. | Paid tiers are ruled out. Partial, labeled results are honest; waiting days is not possible in one session. | Rerun `pnpm eval --model <name>` on later days; `pnpm eval:report` regenerates the tables. |
| 12 | CI: pushes run lint, typecheck, tests. Pull requests also run the 13 safety cases (the job fails unless all pass) and 5 sampled cases (reported, never failing the job). Manual dispatch runs the full suite for a chosen model. Eval steps skip with a notice when the repo has no API key secrets. | CLAUDE.md's CI plan. Sampled cases vary by model, so failing PRs on them would be noise; safety is the hard gate the spec names. | `.github/workflows/ci.yml`. |
| 13 | After eval run 1, fixed what the evals found. Product: planner and keyword routing send counts of past-due or failed items to recovery; alert cards show only when the question is about that rule's own window (the 7 day decline card had contradicted 30 day and per-brand answers); cohort months are written as names. Grading: a negated sentence is no longer a completion claim (the only safety failure, safety-08, was this); one forbidden phrase and two fact lists were too strict; the judge is told demo proposals cannot be confirmed; the judge cache key includes a prompt version. | Each change traces to a specific failed case in `evals/results-run1.md`. Run 1 is kept unchanged as the baseline. | Revert the commit "Fix routing, alert cards, and grading found by eval run 1". |
| 14 | Run 2 reruns only the cases that did not pass in run 1, under the fixed code and grader, into `evals/runs/chain.json`; passing run 1 results are carried over, not rerun. `evals/results.md` says so. | Free tier quota for a full rerun was not available the same day. Carrying passes over can hide a regression the fixes introduced in a passing case; a full `--fresh` run on a later day removes that caveat. | `pnpm eval --model chain --fresh` on a day with quota. |
| 15 | The gpt-oss-120b single-model benchmark was not started. | The production chain run spent 120b's 200,000 daily tokens first; results.md (the app as deployed) mattered more than the benchmark row. | `pnpm eval --model gpt-oss-120b` on a later day. |
| 16 | The demo answer cache was not rewarmed after the prompt and tool changes in this session. | Every model's free quota was used by the eval runs. Until `pnpm demo:warm` runs, demo questions are answered live. | Run `pnpm demo:warm` when quota is back. |
